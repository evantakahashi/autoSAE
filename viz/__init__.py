"""viz — visualization layer for automechresearch.

tour.py       — one-shot tour of the base Pythia-160M (induction heads, attention
                patterns, logit lens, residual stream norms). Does not require a
                trained SAE.
dashboard.py  — (planned) generated after each train_sae.py run.
explore_features.py — (planned) feature browser for a winning SAE.
"""
